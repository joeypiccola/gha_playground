name: "Helm upgarde --install"
description: Run Helm upgrade install

inputs:
  namespace:
    description: ""
    required: true
  version:
    description: ""
    required: true
  chart_name:
    description: ""
    required: true
  chart_source :
    description: ""
    required: true
  directory :
    description: ""
    required: true

runs:
  using: composite
  steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Deploy external-secrets-operator
      shell: bash
      run: |
        tree
      working-directory: k8s-helm-deployments/${{ inputs.directory }}
